; Ручной поиск максимума
; Массив: [5, 3, 8, 1, 9, 2, 7]
; Результат: 9 (максимальный элемент)

; Просто сравниваем каждый элемент с предыдущим максимумом
; и берем больший

; Начинаем с первого элемента
LOAD 201
STORE 100

; Сравниваем со вторым элементом
LOAD 202
CMP 100
JZ SKIP1
; Если 202 > 100, обновляем
LOAD 202
STORE 100
SKIP1:

; Сравниваем с третьим элементом  
LOAD 203
CMP 100
JZ SKIP2
LOAD 203
STORE 100
SKIP2:

; Сравниваем с четвертым элементом
LOAD 204
CMP 100
JZ SKIP3
LOAD 204
STORE 100
SKIP3:

; Сравниваем с пятым элементом (9)
LOAD 205
CMP 100
JZ SKIP4
LOAD 205
STORE 100
SKIP4:

; Сравниваем с шестым элементом
LOAD 206
CMP 100
JZ SKIP5
LOAD 206
STORE 100
SKIP5:

; Сравниваем с седьмым элементом
LOAD 207
CMP 100
JZ SKIP6
LOAD 207
STORE 100
SKIP6:

HALT
